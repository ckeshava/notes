# if no interpreter is specified, the default will be used.
# the default interpreter can be seen via $SHELL command.
# output of echo command will remove all the leaing whitespaces. This is because of the way interpreter executes echo commands. 

echo `date`
echo `cal` # all of the output will be compressed to a single line.